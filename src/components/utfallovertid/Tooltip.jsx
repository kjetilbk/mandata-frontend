import React from 'react';

const Tooltip = ({contentTooltip, updateDateLabel}) => {
  updateDateLabel(contentTooltip.title);
  return (
    <div>
    </div>
  );
}

export default Tooltip;
